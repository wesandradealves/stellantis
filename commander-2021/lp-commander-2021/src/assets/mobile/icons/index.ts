const hash = '/images/icons'

const check = `${hash}/check.png`
const chevronDownBlack = `${hash}/chevron-down-black.svg`
const chevronDownRed = `${hash}/chevron-down-red.svg`
const chevronLeftRed = `${hash}/chevron-left-red.svg`
const whatsappIcon = `teste`

export { check, chevronDownBlack, chevronDownRed, chevronLeftRed, whatsappIcon }
